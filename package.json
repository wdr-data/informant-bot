{
  "name": "tim-bot",
  "version": "0.1.0",
  "repository": "https://github.com/wdr-data/tim-bot",
  "author": "Hacking Studio <github.com/wdr-data>",
  "license": "MIT",
  "private": true,
  "devDependencies": {
    "serverless-domain-manager": "^2.2.1",
    "serverless-offline": "^3.16.0"
  },
  "dependencies": {
    "dialogflow": "^0.1.0",
    "fb-messenger": "^1.2.0"
  },
  "scripts": {
    "deploy": "yarn && sls deploy",
    "postinstall": "npm run rebuild-grpc",
    "rebuild-grpc": "npm rebuild --target=6.1.0 --target_arch=x64 --target_platform=linux --target_libc=glibc"
  }
}
